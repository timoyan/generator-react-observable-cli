# Generator React Observable Cli

## Before execute cli

-   Run `npm install -g yo`, if you are in China,
    please use `npm install -g yo --registry=https://registry.npm.taobao.org/`

## Execute cli

-   Run `yo redux-observable-cli`
